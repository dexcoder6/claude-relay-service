# 演示模式配置说明

演示模式用于限制某些功能，确保演示环境的稳定性和安全性。

## 环境变量配置

### 后端配置

在项目根目录的环境变量文件中配置：

**文件位置**: `.env` 或 `.env.local`

```bash
# 🎭 演示环境配置
# 演示模式开关，启用时会对某些功能进行限制（如批量创建 API Key 限制为 10 个）
DEMO_MODE=true  # 演示环境设置为 true，生产环境设置为 false
```

### 前端配置

在前端项目的环境变量文件中配置：

**文件位置**: `web/admin-spa/.env.local`

```bash
# ========== 演示环境配置 ==========

# 演示模式开关
# 设置为 true 时启用演示环境限制（如批量创建 API Key 限制为 10 个）
# 开发/测试环境可设置为 true，生产环境应设置为 false
VITE_DEMO_MODE=true  # 演示环境设置为 true，生产环境设置为 false
```

## 演示模式限制

当演示模式启用时，以下功能会受到限制：

### 1. 批量创建 API Keys
- **生产环境**: 最大 500 个
- **演示环境**: 最大 10 个
- **说明**: 防止演示环境创建过多 API Key 影响性能

### 2. 自动检测机制

除了环境变量控制，系统还会自动检测演示环境：

#### 后端自动检测
- 检查环境变量 `DEMO_MODE === 'true'`
- 检查请求头 host 是否包含 'demo' 或 'test'

#### 前端自动检测
- 检查环境变量 `VITE_DEMO_MODE === 'true'`
- 检查域名是否包含 'demo' 或 'test'

## 部署建议

### 演示环境部署
```bash
# 后端 .env
DEMO_MODE=true

# 前端 .env.local
VITE_DEMO_MODE=true
```

### 生产环境部署
```bash
# 后端 .env
DEMO_MODE=false

# 前端 .env.local
VITE_DEMO_MODE=false
```

### 使用域名自动检测
如果你的演示站点域名包含 'demo' 或 'test'，如：
- `demo.example.com`
- `test-api.example.com`

系统会自动启用演示模式，无需手动配置环境变量。

## 重启服务

修改环境变量后需要重启相关服务：

```bash
# 重启后端服务
npm run restart

# 重新构建前端（如果是生产模式）
cd web/admin-spa
npm run build
```

## 验证配置

启动服务后，可以通过以下方式验证演示模式是否生效：

1. 访问管理后台的 API Keys 页面
2. 点击"批量创建"
3. 如果看到"演示环境限制 10 个"的提示，说明演示模式已启用
4. 如果显示"最大支持 500 个"，说明为生产模式

## 注意事项

- 环境变量修改后需要重启服务才能生效
- 前端的 `VITE_` 前缀环境变量在构建时被编译，需要重新构建
- 演示模式主要用于限制资源消耗，不影响核心功能
- 建议在生产环境中关闭演示模式以获得完整功能